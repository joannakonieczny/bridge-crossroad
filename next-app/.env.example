# Environment Configuration for Bridge Crossroad

# Session Configuration
# Secret key for session encryption (default: "123" - change in production!)
SESSION_SECRET=your-super-secret-session-key

# Session expiration time in seconds (default: 3600 = 1 hour)
EXPIRATION_TIME=3600

# Whether to use secure cookies (default: false - set to true in production)
SECURE_COOKIES=false

# Database Configuration
# MongoDB connection string (required - no default)
MONGODB_URI=mongodb://localhost:27017

# MongoDB database name (required - no default)
MONGODB_DB_NAME=bridge_crossroad

# AWS S3 Configuration
# AWS Region (e.g., us-east-1)
AWS_REGION=your-aws-region

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# S3 Bucket Name
S3_BUCKET_NAME=your-s3-bucket-name

# URL Expiration Time in Seconds for S3 Signed URLs
URL_EXPIRATION_TIME_SECONDS=60

# AWS Session Token (if using temporary credentials)
AWS_SESSION_TOKEN=your-aws-session-token

# Development vs Production Notes:
# - In production, set SESSION_SECRET to a strong random string
# - In production, set SECURE_COOKIES=true if using HTTPS
# - Use appropriate MongoDB connection string for your environment
